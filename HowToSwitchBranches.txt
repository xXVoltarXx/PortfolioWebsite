repository --> open in command line

for old site
git checkout master
git reset --hard old-site
git push origin master --force


for new site
git checkout master
git reset --hard new-site
git push origin master --force

or just change the netlify deploy branch